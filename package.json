{
  "name": "dockerfile-generator",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist",
    "astro": "astro",
    "sync:registry": "node --experimental-strip-types --no-warnings scripts/sync-registry.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^13.7.0",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.4",
    "jszip": "^3.10.1",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  }
}
